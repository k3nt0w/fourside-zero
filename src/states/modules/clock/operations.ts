import { actions } from './actions'

export const operations = {
  serverRender: actions.serverRender,
  increment: actions.increment,
  decrement: actions.decrement,
  start: actions.start,
  reset: actions.reset
}
