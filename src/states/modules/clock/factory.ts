// TODO(@k3nt0w): support factory seeds

// import { faker, toArray } from '../../../test-utils'
// import { State } from './reducers'

// const user = (): State => ({ name: faker.name.firstName() })

// export const users = toArray(user)
